# StreamBox frontend environment variables
# Copy to `.env` in the frontend folder as needed. All values are OPTIONAL.

# In production, set this to the deployed backend root, e.g.:
# VITE_API_URL=https://streambox-api.onrender.com/api
# In dev (localhost) leave unset so the Vite dev proxy (-> localhost:5000) is used.
